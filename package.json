{
  "name": "lotto-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for 동행복권 API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.91.0"
  }
}
